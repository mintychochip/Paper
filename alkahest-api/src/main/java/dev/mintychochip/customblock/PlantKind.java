package dev.mintychochip.customblock;

/** Growth family used by a custom plant host. */
public enum PlantKind {
    /** Age/stage-based crop carrier. */
    CROP,
    /** Two-stage sapling carrier that can become a structure. */
    SAPLING
}
