--- a/net/minecraft/world/item/component/BundleContents.java
+++ b/net/minecraft/world/item/component/BundleContents.java
@@ -25,9 +_,10 @@
 
 public final class BundleContents implements TooltipComponent {
     public static final BundleContents EMPTY = new BundleContents(List.of());
-    public static final Codec<BundleContents> CODEC = ItemStackTemplate.CODEC.listOf().xmap(BundleContents::new, contents -> contents.items);
+    public static final Codec<BundleContents> CODEC = ItemStackTemplate.CODEC.sizeLimitedListOf(256).xmap(BundleContents::new, contents -> contents.items); // Paper - limit size
     public static final StreamCodec<RegistryFriendlyByteBuf, BundleContents> STREAM_CODEC = ItemStackTemplate.STREAM_CODEC
-        .apply(ByteBufCodecs.list())
+        .apply(ByteBufCodecs.list(256)) // Paper - limit size
+        .apply(ByteBufCodecs::increaseDepth) // Paper - Track codec depth
         .map(BundleContents::new, contents -> contents.items);
     private static final Fraction BUNDLE_IN_BUNDLE_WEIGHT = Fraction.getFraction(1, 16);
     private static final int NO_STACK_INDEX = -1;
@@ -38,6 +_,11 @@
     private final Supplier<DataResult<Fraction>> weight;
 
     private BundleContents(final List<ItemStackTemplate> items, final int selectedItem) {
+        // Paper start - limit size
+        if (items.size() > 256) {
+            throw new IllegalArgumentException("Too many items");
+        }
+        // Paper end - limit size
         this.items = items;
         this.selectedItem = selectedItem;
         this.weight = Suppliers.memoize(() -> computeContentWeight(this.items));
@@ -80,6 +_,13 @@
         return !itemToAdd.isEmpty() && itemToAdd.getItem().canFitInsideContainerItems();
     }
 
+    // Paper start - correct bundle inventory action
+    public int getMaxAmountToAdd(final ItemStack stack) {
+        DataResult<Fraction> weight = this.weight();
+        return weight.isError() ? 0 : Mutable.getMaxAmountToAdd(stack, weight.getOrThrow());
+    }
+    // Paper end - correct bundle inventory action
+
     public int getNumberOfItemsToShow() {
         int numberOfItemStacks = this.size();
         int availableItemsToShow = numberOfItemStacks > 12 ? 11 : 12;
@@ -180,6 +_,14 @@
             return Math.max(remainingWeight.divideBy(itemWeight).intValue(), 0);
         }
 
+        // Paper start - correct bundle inventory action
+        // Static overload to easily compute this value without the need for an instance of mutable.
+        static int getMaxAmountToAdd(final ItemStack stack, final Fraction weight) {
+            Fraction remainingWeight = Fraction.ONE.subtract(weight);
+            return Math.max(remainingWeight.divideBy(BundleContents.getWeight(stack).getOrThrow()).intValue(), 0);
+        }
+        // Paper end - correct bundle inventory action
+
         public int tryInsert(final ItemStack itemsToAdd) {
             if (!BundleContents.canItemBeInBundle(itemsToAdd)) {
                 return 0;
@@ -195,16 +_,44 @@
             if (amountToAdd == 0) {
                 return 0;
             }
-
+            // mintychochip start - item provenance: ensure stack before bundle pack
+            dev.mintychochip.provenance.ItemProvenance.ensure(itemsToAdd, dev.mintychochip.provenance.StackLocation.labeled("bundle"));
+            // mintychochip end - item provenance
             this.weight = this.weight.add(itemWeight.multiplyBy(Fraction.getFraction(amountToAdd, 1)));
             int stackIndex = this.findStackIndex(itemsToAdd);
             if (stackIndex != BundleContents.NO_STACK_INDEX) {
-                ItemStack removedStack = this.items.remove(stackIndex);
-                ItemStack mergedStack = removedStack.copyWithCount(removedStack.getCount() + amountToAdd);
+                final ItemStack removedStack = this.items.remove(stackIndex);
+                // mintychochip start - item provenance: capture both ids before bundle merge
+                final dev.mintychochip.provenance.StackLocation sourceLocation =
+                    dev.mintychochip.provenance.StackLocation.labeled("bundle");
+                final dev.mintychochip.provenance.StackLocation targetLocation =
+                    dev.mintychochip.provenance.StackLocation.labeled("bundle");
+                dev.mintychochip.provenance.ItemProvenance.ensure(removedStack, targetLocation);
+                dev.mintychochip.provenance.ItemProvenance.ensure(itemsToAdd, sourceLocation);
+                final dev.mintychochip.provenance.MergeTransition provenanceMerge =
+                    dev.mintychochip.provenance.MergeTransition.capture(
+                        removedStack,
+                        itemsToAdd,
+                        targetLocation,
+                        sourceLocation
+                    ).orElse(null);
+                final ItemStack mergedStack = removedStack.copyWithCount(removedStack.getCount() + amountToAdd);
                 itemsToAdd.shrink(amountToAdd);
+                if (provenanceMerge != null) {
+                    dev.mintychochip.provenance.ItemProvenance.applyMerge(
+                        provenanceMerge,
+                        mergedStack,
+                        itemsToAdd
+                    );
+                }
+                // mintychochip end - item provenance
                 this.items.add(0, mergedStack);
             } else {
-                this.items.add(0, itemsToAdd.split(amountToAdd));
+                final ItemStack taken = itemsToAdd.split(amountToAdd);
+                // mintychochip start - item provenance
+                dev.mintychochip.provenance.ItemProvenance.ensure(taken, dev.mintychochip.provenance.StackLocation.labeled("bundle"));
+                // mintychochip end - item provenance
+                this.items.add(0, taken);
             }
 
             return amountToAdd;
