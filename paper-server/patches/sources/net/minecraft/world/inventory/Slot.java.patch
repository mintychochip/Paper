--- a/net/minecraft/world/inventory/Slot.java
+++ b/net/minecraft/world/inventory/Slot.java
@@ -142,9 +_,33 @@
             if (slotStack.isEmpty()) {
                 this.setByPlayer(inputStack.split(transferableItemCount));
             } else if (ItemStack.isSameItemSameComponents(slotStack, inputStack)) {
+                // mintychochip start - item provenance: capture both ids before slot merge
+                final dev.mintychochip.provenance.StackLocation sourceLocation =
+                    dev.mintychochip.provenance.StackLocation.labeled("cursor");
+                final dev.mintychochip.provenance.StackLocation targetLocation =
+                    dev.mintychochip.provenance.StackLocation.labeled("menu-slot:" + this.index);
+                dev.mintychochip.provenance.ItemProvenance.ensure(slotStack, targetLocation);
+                dev.mintychochip.provenance.ItemProvenance.ensure(inputStack, sourceLocation);
+                final dev.mintychochip.provenance.MergeTransition provenanceMerge =
+                    dev.mintychochip.provenance.MergeTransition.capture(
+                        slotStack,
+                        inputStack,
+                        targetLocation,
+                        sourceLocation
+                    ).orElse(null);
+                // mintychochip end - item provenance
                 inputStack.shrink(transferableItemCount);
                 slotStack.grow(transferableItemCount);
                 this.setByPlayer(slotStack);
+                // mintychochip start - item provenance
+                if (provenanceMerge != null) {
+                    dev.mintychochip.provenance.ItemProvenance.applyMerge(
+                        provenanceMerge,
+                        slotStack,
+                        inputStack
+                    );
+                }
+                // mintychochip end - item provenance
             }
 
             return inputStack;
